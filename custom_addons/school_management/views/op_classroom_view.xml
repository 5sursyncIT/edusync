<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Action pour les salles de classe -->
    <record id="action_op_classroom" model="ir.actions.act_window">
        <field name="name">Salles de classe</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">op.classroom</field>
        <field name="view_mode">tree,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Créer votre première salle de classe
            </p>
            <p>
                Les salles de classe permettent de gérer les espaces d'enseignement.
            </p>
        </field>
    </record>
</odoo> 